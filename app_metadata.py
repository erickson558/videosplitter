"""Application metadata shared across runtime and build scripts."""

from __future__ import annotations

APP_NAME = "VideoSplitter"
APP_VERSION = "1.1.1"
APP_DISPLAY_VERSION = f"V{APP_VERSION}"
APP_TITLE = f"{APP_NAME} {APP_DISPLAY_VERSION}"